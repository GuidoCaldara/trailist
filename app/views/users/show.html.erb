<div class="container">
  <div class="row mt-5">
    <div class="col-md-3 d-flex flex-column align-items-center py-4">
      <%= image_tag current_user.profile_picture ,class: "d-block avatar avatar-xxl user-prifile-avatar p-2 mb-4" %>
      <h5><%= @user.username %></h5>
      <p class="text-secondary">
        <i class="fa-map-marker-alt fa mr-1"></i> Albino, Bg, Italy
      </p>
      <% if policy(@user).edit? %>
        <%= link_to "Modifica il profilo", edit_user_path(@user) , class: "mb-4 ml-4 btn btn-outline-light" %>
      <% end %>
    </div>
  <div class="col-md-9">
    <div class="border-bottom-light py-4">
      <h4>About</h4>
        <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Fugit iusto, autem debitis eos? Distinctio placeat nam sequi suscipit reprehenderit? Voluptatem, impedit qui non libero. Sed dicta obcaecati dolores ad aliquid!</p>
    </div>
    <div class="border-bottom-light py-4">
      <h4>Recensioni</h4>
        <% if @user.reviews.any? %>
          <% @user.reviews.order('created_at DESC').each do |review|%>
            <%= render "reviews/preview_card" , review: review %>
          <% end %>
        <% else %>
          <p class="text-muted mb-5"><%= @user.username %> Non ha ancora lasciato recensioni</p>
        <% end %>
    </div>

  </div>

  </div>
</div>
